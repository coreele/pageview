# Keys only — do not put real credentials here.
# Prefer DATABASE_URL, or the discrete PG* keys below.

DATABASE_URL=
PGHOST=
PGPORT=
PGDATABASE=
PGUSER=
PGPASSWORD=

# Server bind (defaults shown)
HOST=127.0.0.1
PORT=8787
